# app/utils/__init__.py
# Pure utility functions — no business logic, no DB, no AI.
# Examples: file type detection, text cleaning, slug generation.
# Every function here must be independently testable with no dependencies.
