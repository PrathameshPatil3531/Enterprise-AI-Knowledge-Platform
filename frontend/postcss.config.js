/* PostCSS configuration — required by Tailwind CSS */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // autoprefixer automatically adds vendor prefixes (-webkit-, -moz-)
    // so you write standard CSS and it works across all browsers
  },
}
